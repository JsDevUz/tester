import React,{useCallback,useEffect,useState}from'react';import{Alert,FlatList,Pressable,Text,View}from'react-native';import type{NativeStackNavigationProp}from'@react-navigation/native-stack';import{Radio,ChevronRight}from'lucide-react-native';import type{RootStackParamList}from'../navigation/types';import type{ActiveClass}from'../types/api';import{api}from'../lib/api';import{Empty,Header,Loading,OfflineBanner,Screen}from'../components/Ui';import{useNetwork}from'../providers/NetworkProvider';
export function LiveScreen({navigation}:{navigation:NativeStackNavigationProp<RootStackParamList>}){const[data,setData]=useState<ActiveClass[]>([]);const[loading,setLoading]=useState(true);const{online}=useNetwork();const load=useCallback(async()=>{if(!online){setData([]);setLoading(false);return;}try{const r=await api.get('/classroom/sessions/active');setData(r.data);}finally{setLoading(false);}},[online]);useEffect(()=>{void load();},[load]);return <Screen><OfflineBanner/><Header title="Jonli darslar" subtitle="Real-time sahifalar internet talab qiladi"/>{loading?<Loading/>:<FlatList data={data} keyExtractor={x=>x.id} ListEmptyComponent={<Empty text={online?"Hozir faol dars yo'q":"Jonli darslar offline ishlamaydi"}/>} contentContainerClassName="px-4" renderItem={({item})=><Pressable onPress={()=>{if(!online)return Alert.alert('Internet kerak');navigation.navigate('Web',{path:`/classroom/${item.id}`,title:item.courseName,onlineRequired:true});}} className="mt-3 flex-row items-center rounded-2xl bg-white p-4"><View className="h-12 w-12 items-center justify-center rounded-2xl bg-red-50"><Radio size={22} color="#ef4444"/></View><View className="ml-3 flex-1"><Text className="font-extrabold text-ink">{item.courseName}</Text><Text className="mt-1 text-xs font-semibold text-red-500">● Jonli efir</Text></View><ChevronRight size={18} color="#cbd5e1"/></Pressable>}/>}</Screen>}
